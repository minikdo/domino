\documentclass[10pt]{article}{% load tex_tools %}{% autoescape off %}
  \usepackage{geometry}
  \usepackage{longtable}
  \geometry{verbose,a4paper,tmargin=10mm,bmargin=18mm,lmargin=30mm,rmargin=20mm}
  \setlength\parindent{0pt}

  \begin{document}
  
  % Here are two ways to deal with curly braces, if you don't want spaces around
  % the variable.
  % \title{ {{ title|texify }} }
  % \author{% templatetag openbrace %}{{ author|texify }}}
  % \date{\today}
  % \maketitle

  {\center {\large {{ inventory|texify }} z dnia {{ date|date:"Y-m-d"|texify }} }

  \sffamily

  \footnotesize

  {% if query %}

    \begin{longtable}[l]{|p{1cm}|p{6cm}|c @{ } p{0.5cm}|c|c|}
    \hline
    lp. & nazwa towaru & \makebox[1cm][r]{ilość} & jm & \makebox[2cm][r]{cena brutto} & \makebox[2cm][r]{wartość brutto} \\
    \hline
    % \hline
    {% for item in query %}
      {{ forloop.counter|texify }} &
      {{ item.make__name_print|texify }} &
      {{ item.qty|texify }} &
      {{ item.unit__name|texify }} &
      \makebox[2cm][r]{ {{ item.price|texify }} } &
      \makebox[2cm][r]{ {{ item.total|texify }} } \\
    {% endfor %}

    \end{longtable}

    Zakończono na pozycji nr {{ counter|texify }}. Łączna suma towarów brutto:
    {{ total_sum.piece|add:total_sum.weight|texify }} zł
    
  {% endif %}

  \end{document}
  {% endautoescape %}
